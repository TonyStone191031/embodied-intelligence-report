# code 目录说明

## 用途

`code/` 用于存放本项目内部的辅助脚本，包括但不限于：

1. 图表资产校验
2. 结构化数据检查
3. 批量制图
4. 报告导出与整理辅助工具

## 当前阶段

当前阶段的重点不是一次性搭建复杂工具链，而是先建立最小可复用脚本：

1. 读取 `data/processed/` 中的结构化清单
2. 校验对应资产路径是否存在
3. 生成可读的检查结果
4. 把部分结构化 `.csv` 自动导出为正式 markdown 表

后续如果进入批量制图阶段，应优先在本目录扩展，而不是把临时脚本散落到其他路径。
