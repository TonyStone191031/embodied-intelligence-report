# v0.0 冻结目录说明

## 当前状态

本目录已预留给 `v0.0` 的冻结版本使用。

## 说明

当前 `v0.0` 仍在 `docs/report/current/` 中维护，尚未冻结到本目录。

当 `v0.0` 正文完成并确认后，应将以下内容整理到本目录：

1. 版本总入口文件
2. 章节正文文件
3. 该版本专用的 `figures/`
4. 该版本专用的 `tables/`
5. 该版本专用的 `snippets/`
