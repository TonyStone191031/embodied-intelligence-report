# 当前工作版本说明

## 用途

`docs/report/current/` 用于存放当前正在维护的报告工作版本。

## 当前状态

目前这里承载的是 `v0.0` 的完整初稿与持续精修工作版本。

## 工作规则

1. 新的正文写作、章节扩展、引用补充优先在本目录中进行。
2. 当某一版本完成并确认后，应将稳定内容整理到 `docs/report/versions/<version>/`。
3. 如果后续进入 `v0.1`、`v0.2`，仍然建议先在 `current/` 工作，再冻结。
4. 当前阶段已进入“正文第三轮精修 + 结构化资料沉淀 + 图表体系整理”并行阶段。

## 配套目录

- `figures/`：当前工作版本使用的图
- `tables/`：当前工作版本使用的表
- `snippets/`：当前工作版本使用的代码片段
