# 当前版本修订计划

## 当前轮次

- 目标版本：`v0.2`
- 轮次性质：在 `v0.1` 冻结基线之上的“技术主干深度 + 工程案例 + 图表资产”迭代
- 对应正文目录：`docs/report/latex/current/`（v0.2+ 唯一正文编辑源）
- 上一冻结版本：`docs/report/versions/v0.1/`

## 启动基线

1. `v0.1` 已冻结为可追溯 Markdown 源稿快照；其规划文件已归档到 `docs/planning/archive/v0.1/`。
2. `v0.2` 暂不改变全书部分、章节文件与既有编号体系；若研究表明章节职责必须调整，仍须先回写当前详细大纲并取得确认。
3. v0.1 的 Markdown、研究底稿和可复用图表源文件继续是其可编辑源；从 v0.2 起，LaTeX 是正文唯一可编辑源，PDF 是主要交付物，Word 仅为可选兼容产物。

## 历史冻结版导出补正（v0.1）

1. 问题：`v0.1` 已作 Markdown 源稿快照，但未按 `AGENTS.md` 的版本冻结导出流程生成 `output/exports/v0.1/` 下的 Word/PDF 终稿。
2. 处置：严格从 `docs/report/versions/v0.1/` 作为导出源，运行 `code/export/run_export.py --version v0.1 --source frozen`；不改写冻结正文。
3. 交付与核验：生成合并 Markdown、`docx`、`pdf` 和 manifest，复核文件存在、来源为 frozen、PDF 可打开，并对 Word/PDF 抽样视觉检查。
4. 版本口径：在本任务完成前，`v0.1` 仅是“冻结源稿快照”；本任务完成后，`v0.1` 同时具备可追溯 Markdown 源稿和 `output/exports/v0.1/` 下的可读派生产物。

## 历史冻结版导出补正执行结果（2026-07-11）

1. 已使用 Codex 随附 Python 运行时执行 `code/export/run_export.py --version v0.1 --source frozen`，避免默认 Anaconda 环境缺少 `python-docx` 的依赖问题。
2. 已生成 `report-bundle.md`、`report-bundle.manifest.json`、`embodied-intelligence-report-v0.1.docx` 和 `embodied-intelligence-report-v0.1.pdf`。
3. DOCX 为有效 ZIP 文档，含 52 个表格、22 个嵌入图形和 854 个 Word 数学对象；PDF 为 329 页 A4 文档，文本抽取未发现 `\\frac`、`\\[`、本机绝对路径或 `file:///` 残留。
4. 当前会话无法启动 Microsoft Word COM，因此 PDF 使用既有 ReportLab fallback renderer；首、中、末页已完成 Poppler 渲染抽样。Word 仍按规范作为主可读交付物，PDF 作为辅助打印/分发件。

## Batch T：v0.1 Word/PDF 全面排版修复（2026-07-12）

1. 范围：只修复 `code/export/` 导出基础设施及其测试，不改写 `docs/report/versions/v0.1/` 冻结正文。
2. 重点问题：复杂公式（尤其 `cases`、矩阵、分式和长公式）的 Word 数学对象转换；公式段落异常换行；仍以 LaTeX 文本显示的公式；Markdown 段落、代码、路径和链接的错误拼接；PDF fallback 与 Word 的排版口径差异。
3. 目标：从冻结版重新生成 DOCX/PDF，确保公式优先成为可读数学对象或明确的安全降级文本，正文不出现原始 LaTeX 控制序列；代码、路径和英文标识不被错误并入中文正文；图片、表格、页眉页脚和标题层级保持可读。
4. 退出条件：脚本回归测试通过；DOCX/PDF 均重新生成；DOCX 经过结构审计和渲染抽样；PDF 经过 Poppler 页面渲染、文本残留扫描和页面抽样；所有仍无法在当前环境完成的 Word COM/LibreOffice 检查必须明确记录。

## 本轮目标

1. 对技术主干章开展“方法解释 + 工程案例 + 失败模式 + 可复用图表”的一体化补强，避免只增加评述性文字。
2. 将案例写成可复核的系统剖面：任务边界、输入输出、控制/学习接口、数据与运行时约束、失败恢复、可迁移结论。
3. 将图表从正文中的说明性占位提升为可复用资产：每张图必须有源文件、标题、正文锚点、服务的问题和必要的引用说明。
4. 继续将正文结论沉淀到 `research/notes/`、`research/papers/`、`research/companies/` 或 `data/processed/`，避免后续更新重新从零检索。

## 第一批优先范围

### 技术主干：`05 / 06 / 08 / 13`

1. `05-机器人学的物理、控制与状态估计基础.md`
   - 补“接触丰富操作”的控制、估计与模型失配案例；明确几何、动力学、观测延迟和安全回退如何共同限制策略。
2. `06-学习型机器人与训练闭环基础.md`
   - 补“数据采集 - 离线训练 - 仿真验证 - 真机试运行 - 失败回流”的训练闭环案例；把模仿学习、强化学习和生成式策略放回同一套工程链。
3. `08-具身智能系统架构与分层接口.md`
   - 补“语言任务到受约束执行”的多时间尺度系统案例；明确高层模型、技能层、状态估计、控制回路和安全监督的接口契约。
4. `13-数据工程与训练闭环.md`
   - 补数据血缘、失败片段标注、质量门和主动回采案例；连接数据闭环与部署后性能退化诊断。

### 批次 Q 章节级预审结果

本批不新增正文标题层级，也不调整既有章节职责；案例以现有三级标题内部的“工程案例”段落、配套表格和图表补充形式落地。

1. `05`：围绕 `22.2-22.4` 与 `24.3-24.5` 补“柔顺插装 / 接触丰富操作”案例，重点解释阻抗目标、状态估计、碰撞阈值与回退逻辑的共同约束。
2. `06`：围绕 `27.3-27.4`、`30.3-30.4` 补“示教策略的闭环纠偏与真机失败回流”案例，重点解释分布漂移、训练数据、验证与上线门槛。
3. `08`：围绕 `39.4`、`40.2-40.4`、`41.1-41.4` 补“语言任务到受约束执行”案例，重点解释多时间尺度接口、执行器/中间件调度与安全监督。
4. `13`：围绕 `63.1-63.4`、`65.1-65.4` 补“真实机器人数据血缘与失败回采”案例，重点解释同步、标注、质量门、重训练和回归验证。
5. 图表资产：每章最多新增一张流程/接口图和一张结构化案例表；图表必须直接服务于上述案例，而不是平行装饰。

### 后续两批范围

1. 案例深化：`11 / 15 / 21 / 22 / 23 / 27`，分别覆盖 VLA、部署系统、企业路线、应用单元和长期兴趣专题的证据链。
2. 图表深化：优先服务 `05 / 06 / 08 / 13`；需要时再扩展到 `11 / 15 / 21 / 23 / 27`。先确认正文需要，再新增资产，避免只为凑数量绘图。

## 质量与退出条件

1. 每个目标章至少新增一段完整案例剖析，并写明其不能证明什么。
2. 每个新增图表都能从正文定位到服务的问题、源文件和引用/数据来源；图表不以营销素材替代技术论证。
3. 新增正文经过 `code/analysis/scan_prose_quality.py` 复扫，且不重新引入高风险中英混写、英文尾链或引用悬挂。
4. 每个批次同步形成研究底稿与变更记录；批次完成后才决定下一批是否需要大纲回调。

## 批次 Q 执行结果（2026-07-11）

1. `05` 已补入柔顺插装案例，把阻抗目标、状态估计、碰撞阈值、时延与回退写成同一闭环，并新增接触操作流程图与检查表。
2. `06` 已补入从离线示教到真机纠偏的受控训练闭环，明确 DAgger 式分布偏移、影子评估、灰度运行和版本回归之间的关系，并新增训练放行图与检查表。
3. `08` 已补入语言任务到受约束执行的多时间尺度案例，明确状态包、观测有效年龄、技能终止与安全抢占接口，并新增执行接口图与时间预算表。
4. `13` 已补入真实 episode 血缘与质量门案例，明确同步、标定、失败分类、数据版本与回归验证的关系，并新增数据血缘图与检查表。
5. 新增 Mermaid 图源均已通过仓库渲染器实际渲染检查；`05 / 06 / 08 / 13` 经语言与引用写法复扫，当前脚本口径下问题数为零。

## Batch U：v0.2 LaTeX 主源迁移与 PDF 发布链（2026-07-12）

1. 类型：结构性工程改造；不修改 `docs/report/versions/v0.1/` 冻结正文，不回写现有 v0.1 DOCX/PDF。
2. 目标：建立 `docs/report/latex/current/`，从 v0.2 起以 LaTeX 作为正文唯一编辑源，以 PDF 作为主交付物。
3. 执行：`01/05/20` 试点通过后，已完成 `01~27` 全部章节迁移，并由 `chapters/manifest.tex` 装配。
4. 工程产物：中文 XeLaTeX 模板、章节拆分、frontmatter、引用库、Markdown 到 LaTeX 迁移器、LaTeX 构建入口、PDF 质量检查、工具链记录和版本冻结辅助脚本。
5. 工具链：已安装并记录 TeX Live 2026、XeLaTeX、latexmk 4.88、Biber 2.21 和 Windows 仿宋字体。
6. 验证：完整 PDF 编译成功；`Overfull \\hbox=0`；PDF 原始 LaTeX 控制序列和本机绝对路径残留均为 `0`；标题、cases、长公式、矩阵、长表格、图表和末页已完成 Poppler 渲染抽样。
7. 版本状态：下一步将把 `docs/report/latex/current/` 冻结到 `docs/report/versions/v0.2/latex/`，再从 frozen 源重建最终 PDF。

## 批次 V：v0.2 章节分页与空白页优化（2026-07-13）

1. 类型：非正文版式维护；不改变章节内容、标题职责或引用内容。
2. 问题：上一轮误将二级数字章节映射为 `\\chapter*`，导致 122 个数字章节频繁分页；同时一级标题没有承担 27 个部分的分页职责。
3. 方案：一级标题改为带 `\\clearpage` 的 `\\reportpart`；二级、三级、四级标题分别映射为 `\\section*`、`\\subsection*`、`\\subsubsection*`，均连续排版。
4. 同步范围：更新迁移器、当前 LaTeX 源、v0.2 冻结 LaTeX 源、LaTeX 工作规范和版本修订记录。
5. 质量门：从 frozen 源完整编译；比较修订前后页数和低文本量页面；检查 `\\part*` 残留、空白页、目录、章节起始页、公式/长表格/图表抽样，并重新生成构建 manifest。

## 批次 V 执行结果（2026-07-13）

1. 已按用户澄清后的语义完成修正：一级标题对应 27 个部分并分页；数字章节和小节不自动分页。
2. 已从 frozen 源完整编译，核对 27 个 `\\reportpart` 分页点和数字章节连续排版，不再沿用上一轮 597 页结果。
3. 已释放 Acrobat 文件锁并强制覆盖标准 PDF，manifest 已同步；质量检查包括原始 LaTeX 控制序列、路径残留、页数、分页点和渲染抽样。
4. 最终 PDF 共 484 页；`Overfull \\hbox=0`，原始 LaTeX 控制序列和本机路径残留均为 `0`；旧的临时修订 PDF 已清理。
