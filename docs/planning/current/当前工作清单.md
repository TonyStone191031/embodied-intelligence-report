# 当前工作清单

> 当前工作版：`v0.3`
>
> 当前阶段：批次 Y 写作标准双试点已通过用户确认；等待启动批次 Z1。

## 状态规则

- `planned`：范围已定义，尚未完成章节级预审与资料定位
- `in_progress`：正在执行，必须同步维护研究底稿与质量记录
- `completed`：计划产物和验证均已完成
- `blocked`：缺少外部信息或用户决策，无法进入下一阶段

## 批次 W：v0.2 收口与 v0.3 工作区切换

- 状态：`completed`
- 性质：版本管理与元数据维护，不改写冻结正文。
- 范围：v0.2 frozen/current 对比、PDF/manifest 核验、规划归档、CHANGELOG、v0.3 元数据与工具链状态。
- 已完成：
  1. frozen/current 文件集合与哈希一致性检查。
  2. 484 页 PDF、manifest 哈希、LaTeX/本机路径残留检查。
  3. 封面、中间正文/代码页、末页视觉抽样。
  4. v0.2 三份活跃规划复制到 `docs/planning/archive/v0.2/`。
- 完成结果：冻结说明、active 元数据、CHANGELOG 和工具链状态均已切换并通过 Git diff 边界检查；冻结正文、章节正文和 manifest 无改动。

## 批次 X：v0.3 详细大纲与证据架构

- 状态：`completed`
- 性质：结构性规划；不写正文。
- 范围：10 个部分、38 个章节、References、附录、旧章节迁移映射、页数预算、章节类型与证据资产矩阵。
- 产物：
  - `具身智能报告-当前详细大纲.md`
  - `v0.3-证据与资产规划.md`
  - 本修订计划与工作清单
- 完成结果：38 章连续编号完整，27 个旧部分迁移映射完整，38 章证据预算完整；正文仍由用户确认门阻止启动。

## 用户确认门：v0.3 详细大纲

- 状态：`completed`
- 批准记录：用户于 2026-07-13 明确回复“批准，先试点重写，给我看看效果”。
- 解锁结果：允许启动批次 Y；其余正文批次仍按各自工作清单推进。

## 批次 Y：写作标准双试点

- 状态：`completed`
- 目标章节：
  - 第 6 章“动力学、接触与柔顺控制”
  - 第 23 章“代表性 VLA 的设计比较”，以动作表示为主比较轴并覆盖模型设计全链
- 正文位置：`docs/report/latex/current/chapters-v0.3/`
- 独立装配：新增试点入口与试点引用库；试点验收前不修改全书 `main.tex` 的旧章节装配。
- 研究底稿：为两章分别登记论文、官方文档、代码仓库、commit、许可证、图表改绘依据和事实/判断边界。
- 目标：验证数学推导、算例、改绘论文图、官方源码、实验表、引用和知识/评价分离模板。
- 当前任务：
  1. 核验经典控制/接触文献、VLA 原始论文和官方仓库。
  2. 建立两章证据矩阵、代码锚点和图表来源记录。
  3. 编写独立 LaTeX 试点稿并完成静态质量检查。
  4. 在可用工具链上执行 XeLaTeX/Biber 页面抽样；工具链不可用时记录环境阻塞，不伪报成功。
- 完成条件：两章分别通过内容审查、引用审查、资产授权检查和 LaTeX 页面抽样，再固定全书写法。
- 用户验收：2026-07-13 用户确认第 6、23 章基本达到预期并对质量表示满意；Y3 已将方法固化为后续章节强制规范。

### 批次 Y2：讲解密度与多来源视觉证据补强

- 状态：`completed`
- 用户反馈：保留现有结构与风格；公式、模型和方法需要更细的逐点讲解，且需增加论文原图或服务于理解的多种改绘图。
- 第 6 章任务：
  1. 将关节动力学和操作空间公式补成“符号—假设—推导—算例—工程边界”链条。
  2. 增加接触模式/摩擦锥示意和阻抗—导纳接口对照图；核验是否存在可合法复用的经典原图。
  3. 将插装算例补成可计算的阈值与状态转移示例。
- 第 23 章任务：
  1. 为回归、离散 token、diffusion、flow matching 分别增加机制图、训练路径、推理路径和一个最小数值/序列示例。
  2. 核验 Diffusion Policy、Octo、OpenVLA 与 $\pi_0$ 论文中可服务理解的原图；优先补入 Octo 主干架构图或合规改绘图。
  3. 对模型简介统一增加“输入组织—主干—动作头—训练目标—推理闭环—证据边界”六项拆解。
- 证据与资产任务：在 `research/notes/v0.3-双试点证据与资产记录.md` 增加候选图、图号/页码、许可证据、处理方式与本地资产路径。
- 验收：重新构建独立试点 PDF；引用键与 BibTeX 闭合；新图均有来源/改绘说明；抽样检查四种动作表示、Octo 架构和第 6 章新图所在页。
- 完成结果：两章新增 7 组可复现 PDF/PNG 图（连同原有 2 图共 9 组），第 6 章补齐虚功、能量、接触模式、操作空间与控制接口解释，第 23 章补齐四条动作表示路线和 Octo 模块拆解。Y2 试点经 XeLaTeX 三遍与 Biber 构建为 23 页，17 个引用键闭合，并完成新增图所在页面抽样。

### 批次 Y3：写作方法与论文原图策略固化

- 状态：`completed`
- 用户确认：第 6、23 章基本达到预期，要求将其写法用于全部章节，并评估个人学习场景下复制论文原图的边界。
- 产物：`docs/planning/章节写作质量与证据资产规范.md`。
- 完成结果：形成逐知识点、逐公式、逐模型协议，五类章节组织方式，图/表/代码耦合规则和章节验收表；建立 A–F 级资产分类、规范/私人学习双构建口径，并完成六项试点来源的许可初判。
- 后续约束：Z1–Z5 的章节任务只有通过该规范的核心公式、核心模型、证据资产和构建检查，才允许标为 `completed`。

## 规范维护：技能调用路由

- 状态：`completed`
- 范围：将技能优先级、局部调用原则、完整编排器限制和 `.planning/` 文件职责写入 `docs/planning/项目工作规范.md` 第 10 节；在 `docs/planning/README.md` 增加入口。
- 结果：项目规划与版本链继续保持唯一权威；后续章节批次不得因 skill 默认流程创建平行正文源或平行发布包。

## 工具链恢复：MiKTeX 项目级初始化

- 状态：`completed`
- 范围：恢复 v0.3 的 XeLaTeX、latexmk、Biber、宏包、字体和试点构建能力。
- 当前证据：可执行文件位于 `D:/Program Files/MiKTeX/miktex/bin/x64/`；项目级 `initexmf --report` 已确认四个本地根目录生效，`biblatex`、`ctex`、`gbt7714`、`miktex-misc`、`dvips` 和 `miktex-dvips-bin-x64-2.9` 已安装到 `tmp/miktex/v0.3/install/`。
- 目标路径：`tmp/miktex/v0.3/` 下的项目级 user config、user data、user install、日志和临时缓存。
- 验收：最小中文 LaTeX 文件可编译，Biber 可运行，核心宏包与字体可定位；随后才进入双试点 PDF 构建。
- 当前产物：两章独立试点源稿、试点引用库、两幅自绘图、证据与资产记录已落盘；17 个引用键与 17 条 BibTeX 记录闭合，资产路径和 LaTeX 括号静态检查通过。
- 环境边界：普通沙箱无法读取 `C:/Users/zhuof/AppData/Local/Microsoft/Windows/Fonts`，导致字体缓存返回 Windows API error 5；受控提升权限下的 XeLaTeX/Biber 构建与页面抽样均已完成。
- 工具链进展：已补齐与 ctex 2.6.1 匹配的 `ltxbase`、LaTeX3、`fontspec`、`xecjk`、`hyperref`、`biblatex-gb7714-2015` 及其依赖；已重建 2026-06-01 XeLaTeX 格式。`code/export/export_latex.py` 现会在检测到 MiKTeX 时自动注入项目级根目录。
- 试点构建证据：`tmp/latex-build/v0.3-pilot-batch8/pilot-main.pdf` 已通过 XeLaTeX 三遍 + Biber，18 页；17 个引用键、17 条 BibTeX 记录闭合；日志无未定义引用/交叉引用或 LaTeX 错误。提升权限仅用于绕过本机沙箱对 Windows 用户字体目录的访问限制。

## 批次 Z1：机器人数学与学习基础

- 状态：`planned`
- 目标章节：第 4–13 章。
- 重点：位姿与李群、运动学、接触动力学、规划控制、状态估计、模仿学习、RL、表示学习、生成式策略与 sim2real。

## 批次 Z2：多模态基础、系统架构与 VLA

- 状态：`planned`
- 目标章节：第 14–25 章。
- 重点：Transformer、连接器、VLM/VLA、记忆、世界模型、任务规划、技能控制、模型比较与开源复现。

## 批次 Z3：数据、仿真、评测、部署与安全

- 状态：`planned`
- 目标章节：第 26–30 章。
- 重点：数据血缘、仿真资产、benchmark、ROS 2/硬件、云边端、安全验证与失败回流。

## 批次 Z4：应用、产业与综合判断

- 状态：`planned`
- 目标章节：第 31–38 章。
- 重点：3–5 个完整系统案例、企业证据矩阵、供应链与单位经济性、政策标准、开放问题与可证伪判断。

## 批次 Z5：References、附录与全书质量门

- 状态：`planned`
- 范围：GB/T 7714 References、术语、数据集、代码仓库、图表授权、复现索引和全文构建。
- 完成条件：引用、交叉引用、资产路径、授权、代码版本、重复段落和 PDF 页面检查全部通过。
