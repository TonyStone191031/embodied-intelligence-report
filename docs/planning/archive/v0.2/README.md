# v0.2 规划归档

本目录保存 v0.2 收口时的活跃大纲、版本修订计划和工作清单快照。归档文件记录当时的真实状态；其中批次 R、S 仍为 `planned`，批次 T 仍为 `in_progress`，没有在归档时伪改为完成。

v0.2 的 LaTeX 冻结源位于 `docs/report/versions/v0.2/latex/`，发布 PDF 与构建清单位于 `output/exports/v0.2/`。v0.3 已采用新的结构性重写计划，因此上述未完成批次不再继续修改 v0.2，而由 v0.3 新批次重新定义。

