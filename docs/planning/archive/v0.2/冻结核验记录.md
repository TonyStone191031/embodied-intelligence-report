# v0.2 冻结核验记录

## 冻结边界

- 冻结正文：`docs/report/versions/v0.2/latex/`
- current 基线：`docs/report/latex/current/` 切换到 v0.3 前的状态
- 发布 PDF：`output/exports/v0.2/embodied-intelligence-report-v0.2.pdf`
- 构建清单：`output/exports/v0.2/latex-build-manifest.json`

## 2026-07-13 核验结果

1. 排除冻结目录专有的 `VERSION.json` 后，current 与 frozen 的文件集合及逐文件 SHA-256 完全一致。
2. PDF 为 484 页、3,548,368 字节，SHA-256 为 `57030cbe1bfffba567b92dfd68437a007e0db9895dbc0d84969e222278636a8c`，与成功构建清单一致。
3. `latex_quality_check.py` 静态检查通过；PDF 中原始 LaTeX 控制序列和本机绝对路径残留均为 0。
4. 页 28 仅含目录末尾文本，不是异常空白页。
5. 通过 `pypdfium2` 渲染并抽样封面、正文中部代码页和末页构建说明，未发现乱码、越界或内容缺失。

## 当前工具链限制

冻结时成功构建清单记录了 TeX Live 2026、XeLaTeX、latexmk 4.88 和 Biber 2.21。2026-07-13 复核时，该清单中的 `C:/texlive/2026` 已不存在，系统只发现未完成首次初始化的 MiKTeX。两次重建尝试均在进入正文编译前失败，没有生成或覆盖 PDF；既有 PDF 哈希保持不变，成功构建清单已保留。

该限制不改变 v0.2 已发布文件的冻结状态，但意味着当前机器在修复 TeX 工具链前不能声称“再次从源复建成功”。v0.3 首次正文试点前必须恢复可用的 XeLaTeX/latexmk/Biber 环境并重新记录版本。

## 未完成任务处置

- v0.2 批次 R、S 和 T 保留归档中的原状态。
- 这些任务没有在冻结后继续修改 v0.2，也没有被记为已完成。
- v0.3 的结构性重写、证据架构和新质量门取代其后续执行职责。

